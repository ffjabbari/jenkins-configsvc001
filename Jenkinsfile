def gv

pipeline {

    agent any

    stages {
        stage("init") {
            echo "init-application"
        }
        stage("build") {
            echo "build-application"
        }
        stage("test") {
            echo "tsest-application"
        }
        stage("deploy") {
            echo "deploy-application"
        }
    }   
}